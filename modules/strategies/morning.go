package strategies

// Morning implements IStrategies
func (s *strategy) Morning() {
	// symbol := fmt.Sprintf("%v", s.cfg.NiftyInstrumentToken)
	// res, err := s.data.GetOHLC(symbol)
	// if err != nil {
	// 	s.log.Debug("error to get candles: ", err.Error())
	// 	return
	// }

}
